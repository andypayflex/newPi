# Project Instructions

- Keep responses concise and implementation-focused.
- Prefer the built-in Pi Azure provider before adding custom providers.
- Do not print secrets into tracked files.
- Keep Azure endpoint and model wiring documented in `README.md`.
- After config changes, validate that `.pi/settings.json` and `.env.pi` stay aligned.
